targets: [android]
buildTarget: hello
extraIncludes: [include]
android:
  flavors:
    default: >
      -Dcdep-dependencies_DIR=$(pwd)/smoke-test/.cdep/modules
      -DCMAKE_SYSTEM_VERSION=21
  ndk:
    platforms: [9, 21]

